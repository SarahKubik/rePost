{{#if posts.length}}
<div class="col-6 my-4" id="post-list">
  {{#each posts as |post|}}
  <div class="card">
    {{#if post.trail}}
    <ul class="list-unstyled card-body">
      {{#each post.trail as |trail|}}
      <li class="media">
        <img src="..." class="mr-3" alt="...">
        <div class="media-body">
          <p class="mt-0 mb-1 card-text">Posted by {{trail.name}} on {{format_date trail.createdAt}}</p>
          {{trail.content}}
          <p>---------------</p>
        </div>
      </li>
      {{/each}}
    </ul>
    {{/if}}
    
    <div class="card-body">
      <h5 class="card-title">
          {{post.content}}
      </h5>
    </div>
    <div class="card-text">
      <p>Posted by {{post.user.name}} on {{format_date createdAt}}</p>
    </div>
    {{#if post.follow_tag}}
    <p style="font-style: italic;">Click the post to see more...</p>
    {{/if}}
    <button class="btn col-2 btn-primary reblog" id="{{post.id}}">rePOST</button>
  </div>
  <br>
  {{/each}}
  </div>
{{/if}}


{{!-- {{#if posts.length}}
<div class="card" id="post-list">
   {{#each posts as |post|}}
  <div class="card-content">
    <div class="content">
       {{post.content}}
    </div>
  </div>
  <time>Posted by {{post.user.name}} on {{format_date createdAt}}</time>
  <footer class="card-footer justify-content-center">
    <button class="btn btn-outline-success reblog-btn" id="{{post.id}}" type="button">rePOST</button>
  </footer>
  {{/each}}
</div>
{{/if}} --}}

<script src="./js/reblog-button.js"></script>
